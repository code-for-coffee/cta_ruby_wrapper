# cta_ruby_wrapper
Using Sinatra to expose a CTA wrapper written in Ruby.

I need to split this up a bit and expand on it more... and test it, because I wrote this from memory while on Windows (boo, Windows).
